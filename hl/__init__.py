"""hl — Hyperliquid CLI wrapper.

A standalone CLI around the official Hyperliquid Python SDK
(https://github.com/hyperliquid-dex/hyperliquid-python-sdk). Provides
read-only market data, address-scoped account reads, and signed execution
via an API wallet — including atomic entry + stop-loss + take-profit
("bracket") orders.
"""

from __future__ import annotations

__version__ = "0.1.0"
